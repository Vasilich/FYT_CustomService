<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp">

    <ListView
        android:id="@+id/listTargets"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:choiceMode="singleChoice"
        android:dividerHeight="6dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:orientation="horizontal">

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnAddTarget"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:drawablePadding="6dp"
            app:drawableStartCompat="@drawable/ic_target_add"
            android:text="Add" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnEditTarget"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_weight="1"
            android:drawablePadding="6dp"
            app:drawableStartCompat="@drawable/ic_target_edit"
            android:text="Edit" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnDeleteTarget"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_weight="1"
            android:drawablePadding="6dp"
            app:drawableStartCompat="@drawable/ic_target_delete"
            android:text="Delete" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:orientation="horizontal">

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnMoveUpTarget"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:drawablePadding="6dp"
            app:drawableStartCompat="@drawable/ic_target_up"
            android:text="Move up" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnMoveDownTarget"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_weight="1"
            android:drawablePadding="6dp"
            app:drawableStartCompat="@drawable/ic_target_down"
            android:text="Move down" />
    </LinearLayout>

</LinearLayout>
